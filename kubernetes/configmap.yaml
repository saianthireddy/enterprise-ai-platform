apiVersion: v1
kind: ConfigMap
metadata:
  name: eap-config
data:
  ENVIRONMENT: "production"
  VECTOR_BACKEND: "pinecone"
  PINECONE_INDEX: "enterprise-ai"
  LLM_MODEL: "gpt-4o-mini"
  RATE_LIMIT_PER_MINUTE: "120"
